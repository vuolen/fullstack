The backend is deployed [here](https://vuolen-puhelinluettelo.herokuapp.com/)

If you want to run this, define `MONGODB_URL` and `PORT` as environment variables or in a file called `.env` in the root.
